/**
 * TanStack Table Hooks
 *
 * Re-exports all hooks for convenient importing.
 *
 * @example
 * ```tsx
 * import { useTableState } from '@/components/grid/tanstack/hooks'
 * ```
 */

export { useTableState } from './useTableState'
export type {
  TableState,
  TableStateActions,
  UseTableStateOptions,
  UseTableStateReturn,
} from './useTableState'
