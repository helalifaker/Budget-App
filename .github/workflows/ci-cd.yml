name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

# Required permissions for security scanning (SARIF upload) and PR comments
permissions:
  contents: read
  security-events: write
  actions: read

env:
  NODE_VERSION: '20'
  PYTHON_VERSION: '3.12'

jobs:
  # ===============================
  # Frontend Tests
  # ===============================
  frontend-test:
    name: Frontend Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: pnpm install --frozen-lockfile

      - name: Run ESLint
        working-directory: ./frontend
        run: pnpm lint

      - name: Run TypeScript type check
        working-directory: ./frontend
        run: pnpm typecheck

      - name: Run Vitest unit tests
        working-directory: ./frontend
        run: pnpm test -- --run --coverage

      - name: Upload frontend coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./frontend/coverage/coverage-final.json
          flags: frontend
          name: frontend-coverage

  # ===============================
  # Backend Tests
  # ===============================
  backend-test:
    name: Backend Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: efir
          POSTGRES_PASSWORD: efir_test
          POSTGRES_DB: efir_budget_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "backend/uv.lock"

      - name: Setup Python and Install dependencies
        working-directory: ./backend
        run: |
          uv python install ${{ env.PYTHON_VERSION }}
          uv sync --frozen --all-extras

      - name: Run Ruff linter
        working-directory: ./backend
        run: uv run ruff check .

      - name: Run mypy type checker
        working-directory: ./backend
        run: uv run mypy app

      - name: Run pytest
        working-directory: ./backend
        env:
          DATABASE_URL: postgresql://efir:efir_test@localhost:5432/efir_budget_test
          DIRECT_URL: postgresql://efir:efir_test@localhost:5432/efir_budget_test
        # XML only - HTML reports are local dev only (reduces CI overhead)
        run: uv run pytest --cov --cov-report=xml

      - name: Upload backend coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./backend/coverage.xml
          flags: backend
          name: backend-coverage

  # ===============================
  # E2E Tests (Conditional - main branch or 'run-e2e' label)
  # ===============================
  e2e-test:
    name: E2E Tests (Playwright)
    runs-on: ubuntu-latest
    needs: [frontend-test, backend-test]
    # Skip E2E on PRs unless labeled 'run-e2e' - saves 8-12 min on regular PRs
    if: github.ref == 'refs/heads/main' || contains(github.event.pull_request.labels.*.name, 'run-e2e')
    timeout-minutes: 20  # Prevent 1h+ timeout - E2E should complete in ~10 min

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: efir
          POSTGRES_PASSWORD: efir_test
          POSTGRES_DB: efir_budget_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        working-directory: ./frontend
        run: pnpm exec playwright install --with-deps chromium

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "backend/uv.lock"

      - name: Install backend dependencies
        working-directory: ./backend
        run: |
          uv python install ${{ env.PYTHON_VERSION }}
          uv sync --frozen --all-extras

      - name: Setup auth schema for testing
        working-directory: ./backend
        env:
          DATABASE_URL: postgresql://efir:efir_test@localhost:5432/efir_budget_test
        run: |
          # Create mock auth schema (mimics Supabase auth for E2E tests)
          PGPASSWORD=efir_test psql -h localhost -U efir -d efir_budget_test -f tests/setup_auth_schema.sql

      - name: Start backend server
        working-directory: ./backend
        env:
          DATABASE_URL: postgresql://efir:efir_test@localhost:5432/efir_budget_test
        run: |
          uv run alembic upgrade head
          uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 &
          # Wait for backend health check instead of fixed sleep
          for i in {1..30}; do
            curl -s http://localhost:8000/health && break
            echo "Waiting for backend... ($i/30)"
            sleep 1
          done

      - name: Start frontend server
        working-directory: ./frontend
        env:
          VITE_API_BASE_URL: http://localhost:8000/api/v1
          VITE_E2E_TEST_MODE: 'true'  # Bypass Supabase auth in E2E tests
        run: |
          VITE_E2E_TEST_MODE=true pnpm dev &
          # Wait for frontend health check instead of fixed sleep
          for i in {1..30}; do
            curl -s http://localhost:5173 && break
            echo "Waiting for frontend... ($i/30)"
            sleep 1
          done

      - name: Run Playwright E2E tests
        working-directory: ./frontend
        timeout-minutes: 15  # Individual step timeout
        env:
          CI: 'true'
          VITE_E2E_TEST_MODE: 'true'
        run: pnpm test:e2e

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 30

  # ===============================
  # Build Docker Images (Optional - requires DOCKER_USERNAME and DOCKER_PASSWORD secrets)
  # To enable: Add DOCKER_USERNAME and DOCKER_PASSWORD secrets in GitHub repository settings
  # ===============================
  build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [frontend-test, backend-test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Check Docker credentials
        id: check-docker
        run: |
          if [[ -n "${{ secrets.DOCKER_USERNAME }}" && -n "${{ secrets.DOCKER_PASSWORD }}" ]]; then
            echo "has_credentials=true" >> $GITHUB_OUTPUT
          else
            echo "has_credentials=false" >> $GITHUB_OUTPUT
            echo "⚠️ Docker credentials not configured. Skipping Docker build."
            echo "To enable Docker builds, add DOCKER_USERNAME and DOCKER_PASSWORD secrets."
          fi

      - name: Checkout code
        if: steps.check-docker.outputs.has_credentials == 'true'
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        if: steps.check-docker.outputs.has_credentials == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        if: steps.check-docker.outputs.has_credentials == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push backend image
        if: steps.check-docker.outputs.has_credentials == 'true'
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/efir-budget-backend:latest
            ${{ secrets.DOCKER_USERNAME }}/efir-budget-backend:${{ github.sha }}
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/efir-budget-backend:latest
          cache-to: type=inline

      - name: Build and push frontend image
        if: steps.check-docker.outputs.has_credentials == 'true'
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/efir-budget-frontend:latest
            ${{ secrets.DOCKER_USERNAME }}/efir-budget-frontend:${{ github.sha }}
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/efir-budget-frontend:latest
          cache-to: type=inline
          build-args: |
            VITE_API_BASE_URL=${{ secrets.VITE_API_BASE_URL }}
            VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }}
            VITE_SUPABASE_ANON_KEY=${{ secrets.VITE_SUPABASE_ANON_KEY }}

  # ===============================
  # Deploy to Production (Optional - requires deployment secrets)
  # To enable: Add DEPLOY_HOST, DEPLOY_USER, DEPLOY_SSH_KEY secrets
  # ===============================
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build, e2e-test]
    # Only deploy if this is main branch push AND all deployment secrets are configured
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && always() && needs.e2e-test.result == 'success'
    environment:
      name: production
      url: https://budget.efir-school.com

    steps:
      - name: Check deployment credentials
        id: check-deploy
        run: |
          if [[ -n "${{ secrets.DEPLOY_HOST }}" && -n "${{ secrets.DEPLOY_USER }}" && -n "${{ secrets.DEPLOY_SSH_KEY }}" ]]; then
            echo "has_credentials=true" >> $GITHUB_OUTPUT
          else
            echo "has_credentials=false" >> $GITHUB_OUTPUT
            echo "⚠️ Deployment credentials not configured. Skipping deployment."
            echo "To enable deployment, add DEPLOY_HOST, DEPLOY_USER, DEPLOY_SSH_KEY secrets."
          fi

      - name: Checkout code
        if: steps.check-deploy.outputs.has_credentials == 'true'
        uses: actions/checkout@v4

      - name: Deploy to server
        if: steps.check-deploy.outputs.has_credentials == 'true'
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            cd /opt/efir-budget
            git pull origin main
            docker-compose pull
            docker-compose up -d --no-deps --build
            docker-compose exec -T backend alembic upgrade head

      - name: Run smoke tests
        if: steps.check-deploy.outputs.has_credentials == 'true'
        run: |
          sleep 30
          curl -f ${{ secrets.PRODUCTION_URL }}/health || exit 1
          curl -f ${{ secrets.PRODUCTION_URL }}/api/v1/health || exit 1

      - name: Notify deployment success
        if: success() && steps.check-deploy.outputs.has_credentials == 'true'
        uses: slackapi/slack-github-action@v1.24.0
        with:
          channel-id: ${{ secrets.SLACK_CHANNEL_ID }}
          slack-message: |
            Deployment to production successful!
            Commit: ${{ github.sha }}
            Author: ${{ github.actor }}
            URL: https://budget.efir-school.com
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
        continue-on-error: true  # Don't fail if Slack isn't configured

      - name: Notify deployment failure
        if: failure() && steps.check-deploy.outputs.has_credentials == 'true'
        uses: slackapi/slack-github-action@v1.24.0
        with:
          channel-id: ${{ secrets.SLACK_CHANNEL_ID }}
          slack-message: |
            Deployment to production FAILED!
            Commit: ${{ github.sha }}
            Author: ${{ github.actor }}
            Check: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
        continue-on-error: true  # Don't fail if Slack isn't configured

  # ===============================
  # Security Scan
  # ===============================
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [frontend-test, backend-test]
    # Security scans should not block the pipeline but provide visibility
    continue-on-error: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'
        continue-on-error: true

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'
        continue-on-error: true  # Don't fail if SARIF upload has issues

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Run npm audit
        working-directory: ./frontend
        run: pnpm audit --audit-level=moderate
        continue-on-error: true

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "backend/uv.lock"

      - name: Run Python dependency security check
        working-directory: ./backend
        run: |
          uv python install ${{ env.PYTHON_VERSION }}
          uv pip compile pyproject.toml -o requirements.txt --quiet
          uv run pip-audit -r requirements.txt --progress-spinner off || true
        continue-on-error: true
